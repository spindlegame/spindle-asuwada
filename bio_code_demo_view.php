<?php
// This script and data application were generated by AppGini 5.76
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/bio_code_demo.php");
	include("$currDir/bio_code_demo_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('bio_code_demo');
	if(!$perm[0]){
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "bio_code_demo";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = array(   
		"`bio_code_demo`.`id`" => "id",
		"`bio_code_demo`.`lifespan`" => "lifespan"
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(   
		1 => '`bio_code_demo`.`id`',
		2 => 2
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = array(   
		"`bio_code_demo`.`id`" => "id",
		"`bio_code_demo`.`lifespan`" => "lifespan"
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters = array(   
		"`bio_code_demo`.`id`" => "ID",
		"`bio_code_demo`.`lifespan`" => "Lifespan"
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS = array(   
		"`bio_code_demo`.`id`" => "id",
		"`bio_code_demo`.`lifespan`" => "lifespan"
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array();

	$x->QueryFrom = "`bio_code_demo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = false;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 0;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "bio_code_demo_view.php";
	$x->RedirectAfterInsert = "bio_code_demo_view.php?SelectedID=#ID#";
	$x->TableTitle = "Demographic encoding";
	$x->TableIcon = "table.gif";
	$x->PrimaryKey = "`bio_code_demo`.`id`";

	$x->ColWidth   = array(  150);
	$x->ColCaption = array("Lifespan");
	$x->ColFieldName = array('lifespan');
	$x->ColNumber  = array(2);

	// template paths below are based on the app main directory
	$x->Template = 'templates/bio_code_demo_templateTV.html';
	$x->SelectedTemplate = 'templates/bio_code_demo_templateTVS.html';
	$x->TemplateDV = 'templates/bio_code_demo_templateDV.html';
	$x->TemplateDVP = 'templates/bio_code_demo_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HighlightColor = '#FFF0C2';

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))){ $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])){ // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `bio_code_demo`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='bio_code_demo' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])){ // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `bio_code_demo`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='bio_code_demo' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3){ // view all
		// no further action
	}elseif($perm[2]==0){ // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`bio_code_demo`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: bio_code_demo_init
	$render=TRUE;
	if(function_exists('bio_code_demo_init')){
		$args=array();
		$render=bio_code_demo_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: bio_code_demo_header
	$headerCode='';
	if(function_exists('bio_code_demo_header')){
		$args=array();
		$headerCode=bio_code_demo_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode){
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: bio_code_demo_footer
	$footerCode='';
	if(function_exists('bio_code_demo_footer')){
		$args=array();
		$footerCode=bio_code_demo_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode){
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>