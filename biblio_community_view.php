<?php
// This script and data application were generated by AppGini 5.76
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/biblio_community.php");
	include("$currDir/biblio_community_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('biblio_community');
	if(!$perm[0]){
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "biblio_community";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = array(   
		"`biblio_community`.`id`" => "id",
		"`biblio_community`.`com_name`" => "com_name",
		"`biblio_community`.`description`" => "description"
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(   
		1 => '`biblio_community`.`id`',
		2 => 2,
		3 => 3
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = array(   
		"`biblio_community`.`id`" => "id",
		"`biblio_community`.`com_name`" => "com_name",
		"`biblio_community`.`description`" => "description"
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters = array(   
		"`biblio_community`.`id`" => "ID",
		"`biblio_community`.`com_name`" => "Community name",
		"`biblio_community`.`description`" => "Description"
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS = array(   
		"`biblio_community`.`id`" => "id",
		"`biblio_community`.`com_name`" => "com_name",
		"`biblio_community`.`description`" => "description"
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array();

	$x->QueryFrom = "`biblio_community` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = false;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 0;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "biblio_community_view.php";
	$x->RedirectAfterInsert = "biblio_community_view.php?SelectedID=#ID#";
	$x->TableTitle = "I.1. Communities";
	$x->TableIcon = "resources/table_icons/tower.png";
	$x->PrimaryKey = "`biblio_community`.`id`";

	$x->ColWidth   = array(  150, 150, 150);
	$x->ColCaption = array("ID", "Community name", "Description");
	$x->ColFieldName = array('id', 'com_name', 'description');
	$x->ColNumber  = array(1, 2, 3);

	// template paths below are based on the app main directory
	$x->Template = 'templates/biblio_community_templateTV.html';
	$x->SelectedTemplate = 'templates/biblio_community_templateTVS.html';
	$x->TemplateDV = 'templates/biblio_community_templateDV.html';
	$x->TemplateDVP = 'templates/biblio_community_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HighlightColor = '#FFF0C2';

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))){ $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])){ // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `biblio_community`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='biblio_community' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])){ // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `biblio_community`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='biblio_community' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3){ // view all
		// no further action
	}elseif($perm[2]==0){ // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`biblio_community`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: biblio_community_init
	$render=TRUE;
	if(function_exists('biblio_community_init')){
		$args=array();
		$render=biblio_community_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: biblio_community_header
	$headerCode='';
	if(function_exists('biblio_community_header')){
		$args=array();
		$headerCode=biblio_community_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode){
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: biblio_community_footer
	$footerCode='';
	if(function_exists('biblio_community_footer')){
		$args=array();
		$footerCode=biblio_community_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode){
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>