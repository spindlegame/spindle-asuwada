<?php
// This script and data application were generated by AppGini 5.94
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/bio_author.php");
	include_once("{$currDir}/bio_author_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('bio_author');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout(function() { window.location = "index.php?signOut=1"; }, 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'bio_author';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`bio_author`.`id`" => "id",
		"IF(    CHAR_LENGTH(`bio_team1`.`team`), CONCAT_WS('',   `bio_team1`.`team`), '') /* Equipo */" => "team",
		"IF(    CHAR_LENGTH(`game_agent1`.`id`) || CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`id`, '   ', `game_agent1`.`memberID`), '') /* ID Agente */" => "author_id",
		"IF(    CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`memberID`), '') /* Author memberid */" => "author_memberid",
		"IF(    CHAR_LENGTH(`game_agent1`.`last_name`), CONCAT_WS('',   `game_agent1`.`last_name`), '') /* Apellido(s) */" => "last_name",
		"IF(    CHAR_LENGTH(`game_agent1`.`first_name`), CONCAT_WS('',   `game_agent1`.`first_name`), '') /* Nombre(s) */" => "first_name",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`bio_author`.`id`',
		2 => '`bio_team1`.`team`',
		3 => 3,
		4 => '`game_agent1`.`memberID`',
		5 => '`game_agent1`.`last_name`',
		6 => '`game_agent1`.`first_name`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`bio_author`.`id`" => "id",
		"IF(    CHAR_LENGTH(`bio_team1`.`team`), CONCAT_WS('',   `bio_team1`.`team`), '') /* Equipo */" => "team",
		"IF(    CHAR_LENGTH(`game_agent1`.`id`) || CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`id`, '   ', `game_agent1`.`memberID`), '') /* ID Agente */" => "author_id",
		"IF(    CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`memberID`), '') /* Author memberid */" => "author_memberid",
		"IF(    CHAR_LENGTH(`game_agent1`.`last_name`), CONCAT_WS('',   `game_agent1`.`last_name`), '') /* Apellido(s) */" => "last_name",
		"IF(    CHAR_LENGTH(`game_agent1`.`first_name`), CONCAT_WS('',   `game_agent1`.`first_name`), '') /* Nombre(s) */" => "first_name",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`bio_author`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`bio_team1`.`team`), CONCAT_WS('',   `bio_team1`.`team`), '') /* Equipo */" => "Equipo",
		"IF(    CHAR_LENGTH(`game_agent1`.`id`) || CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`id`, '   ', `game_agent1`.`memberID`), '') /* ID Agente */" => "ID Agente",
		"IF(    CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`memberID`), '') /* Author memberid */" => "Author memberid",
		"IF(    CHAR_LENGTH(`game_agent1`.`last_name`), CONCAT_WS('',   `game_agent1`.`last_name`), '') /* Apellido(s) */" => "Apellido(s)",
		"IF(    CHAR_LENGTH(`game_agent1`.`first_name`), CONCAT_WS('',   `game_agent1`.`first_name`), '') /* Nombre(s) */" => "Nombre(s)",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`bio_author`.`id`" => "id",
		"IF(    CHAR_LENGTH(`bio_team1`.`team`), CONCAT_WS('',   `bio_team1`.`team`), '') /* Equipo */" => "team",
		"IF(    CHAR_LENGTH(`game_agent1`.`id`) || CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`id`, '   ', `game_agent1`.`memberID`), '') /* ID Agente */" => "author_id",
		"IF(    CHAR_LENGTH(`game_agent1`.`memberID`), CONCAT_WS('',   `game_agent1`.`memberID`), '') /* Author memberid */" => "author_memberid",
		"IF(    CHAR_LENGTH(`game_agent1`.`last_name`), CONCAT_WS('',   `game_agent1`.`last_name`), '') /* Apellido(s) */" => "last_name",
		"IF(    CHAR_LENGTH(`game_agent1`.`first_name`), CONCAT_WS('',   `game_agent1`.`first_name`), '') /* Nombre(s) */" => "first_name",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['team' => 'Equipo', 'author_id' => 'ID Agente', ];

	$x->QueryFrom = "`bio_author` LEFT JOIN `bio_team` as bio_team1 ON `bio_team1`.`id`=`bio_author`.`team` LEFT JOIN `game_agent` as game_agent1 ON `game_agent1`.`id`=`bio_author`.`author_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'bio_author_view.php';
	$x->RedirectAfterInsert = 'bio_author_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Bi&#243;grafos';
	$x->TableIcon = 'resources/table_icons/user_edit.png';
	$x->PrimaryKey = '`bio_author`.`id`';

	$x->ColWidth = [150, 150, 150, 150, 150, ];
	$x->ColCaption = ['ID', 'Equipo', 'ID Agente', 'Apellido(s)', 'Nombre(s)', ];
	$x->ColFieldName = ['id', 'team', 'author_id', 'last_name', 'first_name', ];
	$x->ColNumber  = [1, 2, 3, 5, 6, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/bio_author_templateTV.html';
	$x->SelectedTemplate = 'templates/bio_author_templateTVS.html';
	$x->TemplateDV = 'templates/bio_author_templateDV.html';
	$x->TemplateDVP = 'templates/bio_author_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `bio_author`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='bio_author' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `bio_author`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='bio_author' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`bio_author`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: bio_author_init
	$render = true;
	if(function_exists('bio_author_init')) {
		$args = [];
		$render = bio_author_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: bio_author_header
	$headerCode = '';
	if(function_exists('bio_author_header')) {
		$args = [];
		$headerCode = bio_author_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: bio_author_footer
	$footerCode = '';
	if(function_exists('bio_author_footer')) {
		$args = [];
		$footerCode = bio_author_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
