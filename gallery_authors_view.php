<?php
// This script and data application were generated by AppGini 5.94
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/gallery_authors.php");
	include_once("{$currDir}/gallery_authors_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('gallery_authors');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout(function() { window.location = "index.php?signOut=1"; }, 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'gallery_authors';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`gallery_authors`.`id`" => "id",
		"`gallery_authors`.`name`" => "name",
		"`gallery_authors`.`img`" => "img",
		"concat('<i class=\"glyphicon glyphicon-', if(`gallery_authors`.`select`, 'check', 'unchecked'), '\"></i>')" => "select",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`gallery_authors`.`id`',
		2 => 2,
		3 => 3,
		4 => 4,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`gallery_authors`.`id`" => "id",
		"`gallery_authors`.`name`" => "name",
		"`gallery_authors`.`img`" => "img",
		"`gallery_authors`.`select`" => "select",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`gallery_authors`.`id`" => "ID",
		"`gallery_authors`.`name`" => "Name",
		"`gallery_authors`.`select`" => "Select",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`gallery_authors`.`id`" => "id",
		"`gallery_authors`.`name`" => "name",
		"concat('<i class=\"glyphicon glyphicon-', if(`gallery_authors`.`select`, 'check', 'unchecked'), '\"></i>')" => "select",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`gallery_authors` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'gallery_authors_view.php';
	$x->RedirectAfterInsert = 'gallery_authors_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Gallery authors';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`gallery_authors`.`id`';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Name', 'Img', 'Select', ];
	$x->ColFieldName = ['name', 'img', 'select', ];
	$x->ColNumber  = [2, 3, 4, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/gallery_authors_templateTV.html';
	$x->SelectedTemplate = 'templates/gallery_authors_templateTVS.html';
	$x->TemplateDV = 'templates/gallery_authors_templateDV.html';
	$x->TemplateDVP = 'templates/gallery_authors_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `gallery_authors`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='gallery_authors' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `gallery_authors`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='gallery_authors' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`gallery_authors`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: gallery_authors_init
	$render = true;
	if(function_exists('gallery_authors_init')) {
		$args = [];
		$render = gallery_authors_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: gallery_authors_header
	$headerCode = '';
	if(function_exists('gallery_authors_header')) {
		$args = [];
		$headerCode = gallery_authors_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: gallery_authors_footer
	$footerCode = '';
	if(function_exists('gallery_authors_footer')) {
		$args = [];
		$footerCode = gallery_authors_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
