<?php
// This script and data application were generated by AppGini 5.94
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/encounter_scenes1.php");
	include_once("{$currDir}/encounter_scenes1_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('encounter_scenes1');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout(function() { window.location = "index.php?signOut=1"; }, 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'encounter_scenes1';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`encounter_scenes1`.`id`" => "id",
		"`encounter_scenes1`.`scene`" => "scene",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`encounter_scenes1`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`encounter_scenes1`.`id`" => "id",
		"`encounter_scenes1`.`scene`" => "scene",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`encounter_scenes1`.`id`" => "ID",
		"`encounter_scenes1`.`scene`" => "Scene",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`encounter_scenes1`.`id`" => "id",
		"`encounter_scenes1`.`scene`" => "scene",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`encounter_scenes1` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'encounter_scenes1_view.php';
	$x->RedirectAfterInsert = 'encounter_scenes1_view.php?SelectedID=#ID#';
	$x->TableTitle = 'IV.4. Encounter scenes';
	$x->TableIcon = 'resources/table_icons/comments.png';
	$x->PrimaryKey = '`encounter_scenes1`.`id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['ID', 'Scene', ];
	$x->ColFieldName = ['id', 'scene', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/encounter_scenes1_templateTV.html';
	$x->SelectedTemplate = 'templates/encounter_scenes1_templateTVS.html';
	$x->TemplateDV = 'templates/encounter_scenes1_templateDV.html';
	$x->TemplateDVP = 'templates/encounter_scenes1_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `encounter_scenes1`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='encounter_scenes1' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `encounter_scenes1`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='encounter_scenes1' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`encounter_scenes1`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: encounter_scenes1_init
	$render = true;
	if(function_exists('encounter_scenes1_init')) {
		$args = [];
		$render = encounter_scenes1_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: encounter_scenes1_header
	$headerCode = '';
	if(function_exists('encounter_scenes1_header')) {
		$args = [];
		$headerCode = encounter_scenes1_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: encounter_scenes1_footer
	$footerCode = '';
	if(function_exists('encounter_scenes1_footer')) {
		$args = [];
		$footerCode = encounter_scenes1_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
