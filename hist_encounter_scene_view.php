<?php
// This script and data application were generated by AppGini 5.94
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/hist_encounter_scene.php");
	include_once("{$currDir}/hist_encounter_scene_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('hist_encounter_scene');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout(function() { window.location = "index.php?signOut=1"; }, 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'hist_encounter_scene';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`hist_encounter_scene`.`id`" => "id",
		"`hist_encounter_scene`.`scene`" => "scene",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`hist_encounter_scene`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`hist_encounter_scene`.`id`" => "id",
		"`hist_encounter_scene`.`scene`" => "scene",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`hist_encounter_scene`.`id`" => "ID",
		"`hist_encounter_scene`.`scene`" => "Scene",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`hist_encounter_scene`.`id`" => "id",
		"`hist_encounter_scene`.`scene`" => "scene",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`hist_encounter_scene` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'hist_encounter_scene_view.php';
	$x->RedirectAfterInsert = 'hist_encounter_scene_view.php?SelectedID=#ID#';
	$x->TableTitle = 'History encounter scenes';
	$x->TableIcon = 'resources/table_icons/comments.png';
	$x->PrimaryKey = '`hist_encounter_scene`.`id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['ID', 'Scene', ];
	$x->ColFieldName = ['id', 'scene', ];
	$x->ColNumber  = [1, 2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/hist_encounter_scene_templateTV.html';
	$x->SelectedTemplate = 'templates/hist_encounter_scene_templateTVS.html';
	$x->TemplateDV = 'templates/hist_encounter_scene_templateDV.html';
	$x->TemplateDVP = 'templates/hist_encounter_scene_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `hist_encounter_scene`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='hist_encounter_scene' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `hist_encounter_scene`.`id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='hist_encounter_scene' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`hist_encounter_scene`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: hist_encounter_scene_init
	$render = true;
	if(function_exists('hist_encounter_scene_init')) {
		$args = [];
		$render = hist_encounter_scene_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: hist_encounter_scene_header
	$headerCode = '';
	if(function_exists('hist_encounter_scene_header')) {
		$args = [];
		$headerCode = hist_encounter_scene_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: hist_encounter_scene_footer
	$footerCode = '';
	if(function_exists('hist_encounter_scene_footer')) {
		$args = [];
		$footerCode = hist_encounter_scene_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
